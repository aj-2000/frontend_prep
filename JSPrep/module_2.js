// MAP FILTER REDUCE

// 1. map -> return new array, applies a callback to each element, (ele, i, arr) => {...}

// 2. filter -> returns new array with elements which satisfy the filter

// 3. reduce -> reduce entire array to a single element, reduce((acc, ele, i, arr) => {...},0), accumulates -> result of previous computaion

// 4. forEach -> used to modify original array
